name: Rust Proxy Scanner (Ultimate Final Edition)

on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *' # Runs every hour automatically

concurrency:
  group: rust-proxy-scan
  cancel-in-progress: true

jobs:
  build-and-update:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    env:
      CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
      CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
      CF_KV_NAMESPACE_ID: ${{ secrets.CF_KV_NAMESPACE_ID }}
      CF_WORKER_NAME: ${{ secrets.CF_WORKER_NAME }}
      CF_VAR_KEY: ${{ secrets.CF_VAR_KEY }}
      RUST_BACKTRACE: full

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true
          show-progress: false

      - name: Install Rust (latest stable)
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

      - name: Build Rust scanner
        run: |
          echo "⚙️ Building Rust proxy scanner..."
          cargo build --release
          echo "✅ Scanner built successfully."

      - name: Run proxy scanner
        run: |
          echo "🔍 Running proxy scanner..."
          ./target/release/proxy_scanner > output.txt 2>&1 || true
          echo "✅ Scan completed."
          cat output.txt || echo "⚠️ No output file to display."

      - name: Extract Best IP (Safe & Smart)
        shell: bash
        run: |
          echo "🔎 Extracting best IP from output.txt..."

          if [ ! -f output.txt ]; then
            echo "⚠️ output.txt not found, creating empty file..."
            touch output.txt
          fi

          BEST_IP=$(grep -m1 -Eo '([0-9]{1,3}\.){3}[0-9]{1,3}|([a-fA-F0-9:]+:[a-fA-F0-9:]+)' output.txt | head -n 1 | tr -d ' ' || true)

          if [ -z "$BEST_IP" ]; then
            echo "⚠️ No valid IP found — using fallback 1.1.1.1"
            BEST_IP="1.1.1.1"
          fi

          echo "✅ Best IP selected: $BEST_IP"
          echo "BEST_IP=$BEST_IP" >> $GITHUB_ENV

      - name: Install Wrangler CLI (official Cloudflare tool)
        run: |
          npm install -g wrangler
          wrangler --version
          echo "✅ Wrangler installed successfully."

      - name: Authenticate Wrangler
        run: |
          echo "🔐 Configuring Wrangler authentication..."
          export CF_API_TOKEN=${{ secrets.CF_API_TOKEN }}

      - name: Update Cloudflare KV (Auto)
        run: |
          echo "🚀 Updating Cloudflare KV with BEST_IP..."
          wrangler kv:key put \
            --namespace-id="${{ env.CF_KV_NAMESPACE_ID }}" \
            "${{ env.CF_VAR_KEY }}" \
            "${{ env.BEST_IP }}" \
            --account-id="${{ env.CF_ACCOUNT_ID }}" || {
              echo "⚠️ KV update failed, retrying..."
              sleep 5
              wrangler kv:key put \
                --namespace-id="${{ env.CF_KV_NAMESPACE_ID }}" \
                "${{ env.CF_VAR_KEY }}" \
                "${{ env.BEST_IP }}" \
                --account-id="${{ env.CF_ACCOUNT_ID }}"
            }
          echo "✅ KV updated successfully."

      - name: Verify KV value
        run: |
          echo "🔎 Verifying KV value..."
          wrangler kv:key get \
            --namespace-id="${{ env.CF_KV_NAMESPACE_ID }}" \
            "${{ env.CF_VAR_KEY }}" \
            --account-id="${{ env.CF_ACCOUNT_ID }}" || echo "⚠️ Verification skipped (read-only token)."
          echo "✅ KV verification complete."

      - name: Job finished
        run: echo "🎉 Rust Proxy Scanner Workflow finished successfully with IP: $BEST_IP"
